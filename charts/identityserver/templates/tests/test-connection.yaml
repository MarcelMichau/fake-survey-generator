apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "identityserver.fullname" . }}-test-connection"
  labels:
{{ include "identityserver.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "identityserver.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
